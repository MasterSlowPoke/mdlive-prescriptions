<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
  	<%= "<span class=active>".html_safe if @current_count > 0 %>
    <%= @current_count %>
    <span class="glyphicon glyphicon-envelope"></span> 
  	<%= "</span>".html_safe if @current_count > 0 %>
  </a>
  <ul class="dropdown-menu current-reminders" role="menu">
    <% if @current_count > 0 %>
      <h5>You currently have <%= @current_count %> Reminder active:</h5>
      <%= render partial: 'dashboard/doses', locals: { doses: @current_doses } %>
    <% else %>
    	You have no Reminders active right now.
    <% end %>
  </ul>
</li>